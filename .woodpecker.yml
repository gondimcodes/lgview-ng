kind: pipeline
name: test-and-release

steps:
  test:
    image: rust:latest
    commands:
      - cargo test --workspace -j 1
    when:
      event: [push, pull_request]

  build:
    image: rust:latest
    commands:
      - cargo build --release
      - mkdir -p dist
      - cp target/release/lgview-ng dist/lgview-ng
      - cd dist
      - tar -czf lgview-ng-$${CI_COMMIT_TAG}-x86_64-unknown-linux-gnu.tar.gz lgview-ng
    when:
      event: tag

  publish:
    image: woodpeckerci/plugin-release
    settings:
      api_key:
        from_secret: codeberg_token
      base_url: https://codeberg.org
      files:
        - dist/lgview-ng-*.tar.gz
    when:
      event: tag
